NEXT_PUBLIC_COMPANY="KeyFate"
NEXT_PUBLIC_ENV=staging
NEXT_PUBLIC_PARENT_COMPANY="Aviat, LLC"
NEXT_PUBLIC_SITE_URL=https://staging.keyfate.com
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_51RrR62RanDIU40YVFMjG7D718fANj5qHzMJJ2joRw1xa4Y8bGQ3TWflUVQ6mIt0HuuMPf2asZ6jhSV0VpeQWM0Eg00xJlCr8IX
NEXT_PUBLIC_SUPPORT_EMAIL=support@aviat.io
NEXT_PUBLIC_AUTH_PROVIDER=google
NEXT_PUBLIC_DATABASE_PROVIDER=cloudsql
NEXT_PUBLIC_BTCPAY_SERVER_URL=https://btcpay.keyfate.com

# NextAuth configuration (needed for build even though not NEXT_PUBLIC)
NEXTAUTH_URL=https://staging.keyfate.com
NEXTAUTH_SECRET="dummy-secret-for-build"

# Google OAuth (dummy values for build - real ones come from Secret Manager at runtime)
GOOGLE_CLIENT_ID="dummy-client-id-for-build"
GOOGLE_CLIENT_SECRET="dummy-client-secret-for-build"

# Database URL (dummy for build - real one comes from Secret Manager at runtime)
DATABASE_URL="postgresql://dummy@localhost/dummy"

# Encryption key (dummy for build - real one comes from Secret Manager at runtime)
ENCRYPTION_KEY="dummy-encryption-key-for-build"

# Stripe (dummy for build - real one comes from Secret Manager at runtime)
STRIPE_SECRET_KEY="sk_test_dummy"

# BTCPay (dummy values for build - real ones come from Secret Manager at runtime)
BTCPAY_API_KEY="dummy-btcpay-api-key"
BTCPAY_SERVER_URL=https://btcpay.keyfate.com
BTCPAY_STORE_ID="dummy-btcpay-store-id"
BTCPAY_WEBHOOK_SECRET="dummy-btcpay-webhook-secret"

# SendGrid (optional)
SENDGRID_ADMIN_EMAIL=
SENDGRID_API_KEY=
SENDGRID_SENDER_NAME=
